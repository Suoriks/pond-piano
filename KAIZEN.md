# Kaizen log

## 2026-07-12 — 0001 · Северная звезда

**Маленькая цель:** превратить образ «пруд-пианино» в проверяемую модель продукта до написания случайного кода.

Сделано:
- зафиксирована пространственная модель звука X/Y + жест;
- выбрана первая оболочка: offline-first PWA;
- разделены gesture, mapping, audio, renderer и shell;
- записаны дизайн-ограничения и первые риск-прототипы.

**Проверка:** документы согласованы: все платформы покрываются одной первой оболочкой, звук не связан с renderer, accessibility и iOS lifecycle входят в фундамент.

**Следующий маленький шаг:** статичный интерактивный визуальный этюд одного касания — без аудио: точка, удар, затухающие круги, mouse/touch/pen, reduced motion.

## 2026-07-12 — 0002 · Первый удар по воде

**Маленькая цель:** сделать первый живой визуальный контакт с прудом без аудио и зависимостей.

Сделано:
- полноэкранная сумеречная поверхность воды на Canvas 2D;
- Pointer Events для мыши, пальца и пера, `touch-action: none`;
- касание создаёт локальный свет и три затухающих эллиптических кольца;
- давление пера влияет на размах, при отсутствии pressure используется fallback;
- ограничено число активных волн и DPR для мобильной производительности;
- `prefers-reduced-motion` сокращает анимацию до одного короткого кольца;
- добавлен доступный статус первого отклика воды.

**Проверка:** Chromium 1280×800, два синтетических pointer-контакта; Canvas 1280×800, `has-played=true`, live-status обновился, `touch-action=none`, ошибок страницы нет. Снимок: `output/pond-piano/ripple-study-01.png`.

**Следующий маленький шаг:** проверить визуальный этюд на узком мобильном viewport и добавить непрерывный след при скольжении, не превращая воду в рисовалку.

## 2026-07-12 — 0003 · Борозда скольжения

**Цель:** превратить движение удерживаемого касания в цельный водный жест и проверить его в мобильной геометрии.

Сделано:
- добавлен полный lifecycle нескольких Pointer Events с capture, cancel и coalesced samples;
- движение оставляет не линию, а парную расходящуюся борозду, чья ширина зависит от давления и скорости;
- след дискретно семплируется, быстро растворяется и имеет жёсткий лимит памяти;
- вход и выход жеста отмечены волнами разной силы;
- reduced-motion получил более редкий и короткий след вместо полного исчезновения отклика;
- текст первого приглашения теперь прямо предлагает провести по воде.

**Проверка:** `node --check`; Chromium в мобильном viewport 390×844 @2x, жест из 12 сегментов, Canvas 780×1688, `has-played=true`, `touch-action=none`, ошибок страницы нет. Снимок: `output/pond-piano/glide-study-mobile-02.png`. Автоматическая vision-инспекция была недоступна из-за сбоя авторизации image-модели; снимок сохранён для ручного сравнения.

**Следующий шаг:** связать место первого касания с одним мягким Web Audio голосом и дать скольжению непрерывно менять высоту без ступенчатых клавиш.

## 2026-07-14 — 0004 · Вода обрела голос

**Цель:** закончить первый играбельный водный голос, в котором место и движение касания непосредственно формируют звук.

Сделано:
- первое касание после явного пользовательского жеста создаёт мягкий двухчастичный Web Audio голос с безопасной общей компрессией;
- горизонтальное скольжение непрерывно ведёт частоту в диапазоне трёх октав, без скрытых клавиш и ступеней;
- вертикальное движение меняет глубину фильтра и долю светлого обертона;
- давление, где оно доступно, управляет интенсивностью, а release растворяет голос вместе с последней рябью;
- архитектура уже полифонична: каждому активному pointer соответствует независимый голос;
- пруд получил полный клавиатурный путь: `Space`/`Enter` удерживает голос, стрелки двигают его, focus хорошо виден;
- добавлена короткая инструкция локального запуска.

**Проверка:** `node --check`, валидный `state.json`; Chromium в мобильном viewport 390×844 @2x с настоящим AudioContext — pointer-glide/release и keyboard-glide/release без ошибок страницы; отдельный инструментированный Web Audio smoke-test подтвердил ретюнинг обоих осцилляторов, фильтра и остановку голосов; reduced-motion smoke-test прошёл. Снимок: `output/pond-piano/audio-gesture-mobile-03.png`. Автоматическая vision-инспекция снова недоступна из-за ошибки авторизации image-модели.

**Следующий шаг:** превратить этюд в настоящую offline-first PWA: manifest, service worker, иконки и проверка запуска без сети.

## 2026-07-14 — 0005 · Пруд остаётся рядом

**Цель:** превратить играбельный этюд в полностью устанавливаемую offline-first оболочку и доказать её работу без сети.

Сделано:
- добавлен валидный standalone Web App Manifest с русским названием, цветами пруда и `any`/`maskable` иконками;
- создан воспроизводимый набор водных launcher-иконок 180/192/512 px, без букв и интерфейсной символики;
- service worker заранее сохраняет весь app shell, обновляет навигацию из сети и надёжно откатывается к локальной странице;
- добавлены iOS standalone metadata и Apple touch icon;
- регистрация оболочки не вмешивается в аудио или жесты и деградирует без ошибки в браузере без Service Worker;
- README и архитектурная гипотеза обновлены по фактически доказанному поведению.

**Проверка:** `node --check` для приложения и service worker, JSON-проверка manifest/state, размеры всех PNG; Chromium 390×844 @2x зарегистрировал service worker и кэш из 9 ресурсов, сохранил работающий pointer-жест, затем в offline-режиме полностью перезагрузил управляемую страницу с Canvas 780×1688 без ошибок. CDP не нашёл ошибок manifest или installability. Снимок: `output/pond-piano/offline-pwa-mobile-04.png`. Vision-инспекция недоступна из-за ошибки авторизации image-модели; снимок сохранён.

**Следующий шаг:** доказать настоящий полифонический жест: два независимых касания как аккорд, с читаемым пересечением волн и ограничением голосов для слабых телефонов.

## 2026-07-16 — 0006 · Вода держит аккорд

**Цель:** закончить настоящий multi-touch аккорд с независимыми голосами, видимой водной интерференцией и жёстким бюджетом для слабых телефонов.

Сделано:
- каждое из одновременно удерживаемых касаний ведёт собственный Web Audio голос и свободно скользит по высоте и глубине;
- между звучащими касаниями возникают три световых узла и мягкая линза стоячей волны — связь читается как интерференция воды, не как линия интерфейса;
- удерживаемые ноты сохраняют живые контактные кольца, причём низкие тона заметно шире высоких, так что высота различается не одним цветом;
- полифония ограничена шестью голосами вместе с release-хвостами; седьмое касание остаётся полноценным визуальным жестом и получает доступное объяснение лимита;
- master gain плавно нормализуется по числу звучащих голосов, поэтому аккорд не превращается в скачок громкости;
- offline cache поднят до новой версии, а PWA metadata больше не создаёт ошибок или deprecated-предупреждений Chromium.

**Проверка:** `node --check`, JSON и `git diff --check`; Chromium 390×844 с настоящим AudioContext разыграл двухпальцевый аккорд и glissando, затем семь одновременных касаний создали ровно 12 осцилляторов — шесть двухчастичных голосов — без runtime/log errors. Ручная инспекция снимка `output/pond-piano/chord-interference-mobile-05.png` подтвердила два читаемых центра волн и три органичных узла между ними без dashboard-геометрии. Отдельный offline smoke-test загрузил shell v2 из кэша (9 ресурсов), сохранил аккорд и обратную связь при `prefers-reduced-motion` без ошибок. Автоматическая vision-инспекция недоступна из-за ошибки авторизации image-модели.

**Следующий шаг:** исследовать мягкое притяжение к строю после короткой выдержки, показывая доступные высоты течениями/каустикой, а не сеткой клавиш.

## 2026-07-18 — 0007 · Течения находят высоту

**Цель:** закончить жест выдержки: свободно скользящий звук должен мягко найти устойчивую высоту, а вода — показать ближайшие ноты без клавиш и сетки.

Сделано:
- движение сохраняет непрерывную высоту, но после 360 мс покоя голос постепенно тянется к ближайшей ноте трёхоктавной пентатоники;
- притяжение намеренно остаётся мягким и сразу исчезает при новом движении, поэтому жест не защёлкивается в квантайзер;
- рядом с удерживаемым касанием проявляются только три локальных течения-каустики: выбранное и два соседних, без постоянной разметки поверхности;
- один и тот же жест работает для pointer и клавиатуры, включая видимый удерживаемый клавиатурный контакт и доступное live-объяснение;
- при `prefers-reduced-motion` каустики остаются статичными и читаемыми;
- музыкальное отображение вынесено в чистый `pond-music.js` и покрыто отдельным Node-тестом; offline shell обновлён до v3.

**Проверка:** `node --check`, unit-тест отображения и `git diff --check`; Chromium 390×844 @2x с настоящим AudioContext удержал pointer-голос до появления течения, загрузил 10 ресурсов shell v3 и полностью перезагрузился offline без runtime/console errors. Отдельный desktop Chromium smoke-test подтвердил удержание, движение и release с клавиатуры, видимый focus и статичную обратную связь в reduced-motion. Ручная инспекция снимка `output/pond-piano/pitch-currents-mobile-06.png` подтвердила локальные органичные каустики, читаемый центр касания и отсутствие сетки/обрезанного UI. Автоматическая vision-инспекция недоступна из-за ошибки авторизации image-модели.

**Следующий шаг:** превратить затухающие следы отпущенных нот в спокойную водную партитуру — короткую память фразы, различающую высоту и глубину без нотного стана.

## 2026-07-20 — 0008 · Вода помнит фразу

**Цель:** закончить короткую водную партитуру, в которой отпущенные pointer- и клавиатурные жесты сохраняют путь, высоту, глубину и длительность без нотного стана или timeline.

Сделано:
- каждый реально прозвучавший жест после отпускания оставляет на 18 секунд спокойный световой след своей траектории и конечные кольца;
- положение следа сохраняет высоту и глубину напрямую, низкий звук остаётся шире высокого, а одно, два или три конечных кольца различают короткую, среднюю и долгую ноту без зависимости только от цвета;
- несколько следов складываются в короткую фразу и исчезают сами; память ограничена 12 жестами, активный жест — 96 сэмплами, а сохранённый — 24 точками;
- pointer cancel/lost capture, blur и keyboard release корректно завершают и голос, и его водную память;
- `prefers-reduced-motion` убирает дрейф следа и сокращает память до 9 секунд, не удаляя партитуру;
- чистая модель памяти вынесена в `pond-score.js`, покрыта Node-тестом и включена в offline shell v4.

**Проверка:** синтаксис всех JS, unit-тесты музыкального отображения и партитуры, JSON и `git diff --check` прошли. Chromium 390×844 @2x доверенным pointer-жестом разыграл и отпустил настоящий Web Audio glissando: появился след фразы, Canvas 780×1688, shell v4 сохранил 11 ресурсов с `pond-score.js`, затем полностью загрузился offline без runtime/console errors. В offline reduced-motion режиме клавиатурный hold/move/release сохранил фразу и focus. Ручная инспекция `output/pond-piano/water-score-mobile-07.png` подтвердила непрерывную органичную борозду, читаемые конечные кольца, свободную воду без сетки и отсутствие обрезки. Автоматическая vision-инспекция снова недоступна из-за ошибки извлечения account ID у image-модели.

**Следующий шаг:** группировать отдельные жесты в мотивы по паузам и перекрытию, чтобы вода различала одну фразу от следующей без transport-кнопок и временной шкалы.

## 2026-07-20 — 0009 · Пауза делит мотивы

**Цель:** закончить фразировку воды: объединять близкие и перекрывающиеся жесты в мотивы, а после выразительной паузы оставлять новый мотив отдельным — без transport-кнопок, сетки и временной шкалы.

Сделано:
- `pond-score.js` теперь чисто и детерминированно группирует жесты по временному перекрытию и паузе 1,4 секунды;
- группировка не зависит от порядка отпускания нот: долгий удерживаемый голос правильно связывает все жесты, которые с ним пересекались;
- окончания жестов внутри одного мотива соединяет слабое изогнутое подводное течение; между разными мотивами связи нет;
- новый мотив после паузы получает понятное live-объявление, а описание клавиатурного пути объясняет ту же модель;
- память по-прежнему ограничена 12 жестами, фразировка не создаёт DOM/Audio-объектов и сохраняется в reduced-motion как неподвижная связь;
- offline shell обновлён до v5.

**Проверка:** синтаксис JS, оба набора Node-тестов, JSON и `git diff --check` прошли; тесты покрывают короткую паузу, разрыв на новый мотив, обратный порядок отпускания аккорда и долгую ноту-мост. Chromium 390×844 @2x разыграл две пары жестов через паузу: граница мотива объявилась, внутри каждой пары видна отдельная изогнутая связь, Canvas 780×1688, shell v5 сохранил 11 ресурсов и полностью загрузился offline без runtime/console errors. Desktop reduced-motion сохранил клавиатурную игру и focus. Ручная инспекция `output/pond-piano/motif-groups-mobile-08.png` подтвердила два несвязанных водных мотива, свободную от сетки поверхность и отсутствие обрезки; автоматическая vision-инспекция недоступна из-за ошибки извлечения account ID у image-модели.

**Следующий шаг:** дать воде настоящую динамику на устройствах без pressure: использовать скорость первых движений как безопасный fallback для силы атаки и визуального всплеска, не ломая перо и спокойные удержания.

## 2026-07-20 — 0010 · У жеста появилась атака

**Цель:** закончить выразительную атаку воды: на устройствах без pressure выводить силу звука и всплеска из первого движения, сохранив настоящее давление пера, спокойные касания и единую память жеста.

Сделано:
- чистая модель различает настоящее pressure пера/сенсорного экрана и стандартное fallback-значение `.5` Pointer Events;
- без pressure неподвижный контакт начинается мягко, а нормализованная скорость в первые 240 мс поднимает Web Audio envelope до безопасного потолка; позднее glissando больше не перезапускает атаку;
- настоящее давление имеет приоритет над скоростью, поэтому быстрое движение лёгким пером остаётся лёгким;
- сильный начальный жест создаёт дополнительную волну и короткий направленный веер капель, а `prefers-reduced-motion` оставляет один неподвижный отблеск;
- найденная интенсивность сохраняется в водной партитуре, поэтому отпущенный жест помнит не только путь и длительность, но и характер входа;
- обновлены доступное описание и live-объяснение первого сильного взмаха; offline shell поднят до v6.

**Проверка:** синтаксис всех JS, оба набора Node-тестов, JSON и `git diff --check` прошли; тесты покрывают fallback pressure, настоящее pen/touch pressure, спокойный/средний/быстрый жест и потолок скорости. Chromium 390×844 @2x с настоящим AudioContext показал peak `0.0788` у неподвижного контакта и `0.1349` у быстрого взмаха, выдал доступное сообщение о всплеске и не записал runtime/console errors. Keyboard path, reduced-motion и полная offline-перезагрузка из shell v6 прошли. Ручная инспекция `output/pond-piano/expressive-attack-mobile-09.png` подтвердила направленный веер капель, целый мобильный layout и спокойную органичную композицию; автоматическая vision-инспекция недоступна из-за ошибки извлечения account ID у image-модели.

**Следующий шаг:** дать удерживаемому голосу едва живую водную фактуру — глубинный дрейф и мягкое биение, которые развиваются со временем, не маскируют высоту и укладываются в текущий мобильный аудиобюджет.

## 2026-07-20 — 0011 · Удержанная нота дышит

**Цель:** закончить живую фактуру удержания: дать голосу медленное глубинное движение, которое созревает после атаки, следует глубине, не раскачивает высоту и остаётся в текущем мобильном бюджете.

Сделано:
- через 620 мс спокойного удержания один sub-audio oscillator на голос постепенно раскрывает очень малую модуляцию cutoff и уровня обертона; основная частота остаётся нетронутой;
- глубокая вода дышит медленнее, светлая — чуть быстрее и шире по фильтру; вертикальное движение безопасно перенастраивает уже растущую фактуру;
- управляющий oscillator останавливается вместе с двумя звуковыми oscillator nodes на release и подчиняется прежнему лимиту шести голосов;
- у удерживаемого контакта проявляются две открытые подводные струи вместо ещё одного замкнутого кольца; в reduced motion они остаются статичными;
- чистая `heldTexture()` задаёт единые ограниченные параметры звука и рисунка и покрыта unit-тестами;
- доступное live-сообщение сообщает о созревшем глубинном течении, offline shell обновлён до v7.

**Проверка:** синтаксис всех JS, тесты музыкального отображения/фактуры и партитуры, JSON и `git diff --check` прошли. Chromium 390×844 @2x с настоящим AudioContext удержал и перенёс ноту по глубине: создано ровно три oscillator nodes — два звуковых и один управляющий с частотой 0,1477 Гц; после release остановились все три. Семь одновременных touch-контактов по-прежнему создали только шесть голосов — 18 oscillator nodes — и освободили все 18. Shell v7 сохранился и полностью перезагрузился offline; reduced-motion сохранил статичную фактуру; runtime/console errors нет. Прямая инспекция `output/pond-piano/held-undertow-mobile-10.png` и `held-undertow-reduced-mobile-10.png` подтвердила свободную органичную воду, две ненавязчивые открытые струи и целый мобильный layout. Автоматическая vision-инспекция была недоступна из-за ошибки извлечения account ID у image-модели.

**Следующий шаг:** сделать водную партитуру действительно частью инструмента: намеренный новый жест, пересекающий затухающий след, должен один раз мягко отозваться сохранённой высотой и глубиной — с явным cooldown, чтобы не создать feedback-машину.

## 2026-07-21 — 0012 · След отвечает прикосновению

**Цель:** закончить играбельный отклик партитуры: намеренный новый жест через затухающий путь должен один раз мягко разбудить его сохранённую высоту и глубину, не создавая feedback-машину.

Сделано:
- чистая экранная геометрия в `pond-score.js` находит реальное пересечение или близкое касание нового сегмента с видимой памятью жеста, учитывая ширину/высоту viewport и отбрасывая микродрожание;
- после 22 px намеренного движения пересечённый путь рождает короткий тихий Web Audio голос именно из сохранённых pitch/depth и проводит световой импульс к окончанию следа;
- одна память защёлкивается на весь текущий жест, а общий cooldown 3,2 секунды действует между разными жестами; эхо не сохраняется в партитуру и не может разбудить само себя;
- отклик подчиняется прежнему лимиту шести голосов, освобождает все три oscillator nodes после release и сообщает действие через live-status;
- pointer и клавиатурный путь используют одну модель; `prefers-reduced-motion` сохраняет статичное свечение окончания вместо бегущего импульса;
- offline shell обновлён до v8, инструкция и доступное описание объясняют новый жест.

**Проверка:** синтаксис всех JS, музыкальные и score unit-тесты, JSON и `git diff --check` прошли; новые тесты покрывают пересечение, промах, истёкшую память и микродрожание. Chromium 390×844 @2x разыграл горизонтальный след и пересёк его новым pointer-жестом: создан ровно один дополнительный трёхосцилляторный голос, повторное пересечение тем же жестом и новый жест в cooldown не дали второго эха, после release остановились все 12 созданных oscillator nodes. Shell v8 сохранился и полностью перезагрузился offline без runtime/console errors. Отдельный reduced-motion smoke-test подтвердил тот же жест с клавиатуры и сохранённый focus. Прямая инспекция `output/pond-piano/playable-score-echo-mobile-11.png` показала читаемый световой отклик на пересечении, свободную воду без сетки и целый мобильный layout; автоматическая image-инспекция снова недоступна из-за ошибки извлечения account ID.

**Следующий шаг:** дать каждому голосу мягкое пространственное место: X должен вести не только высоту, но и ограниченную stereo-позицию с mono fallback, чтобы активные ноты и пробуждённые следы звучали там, где видны.

## 2026-07-21 — 0013 · Звук живёт там, где вода

**Цель:** закончить пространственное место голоса: активные касания, клавиатура и пробуждённые следы должны мягко звучать там, где видны, без широкой headphone-демонстрации и без потери инструмента в mono-браузере.

Сделано:
- X теперь ведёт не только непрерывную высоту, но и ограниченную стереопозицию ±0,68; центр воды остаётся строго в центре, а glissando перемещается с коротким сглаживанием 45 мс;
- каждый голос получает один `StereoPannerNode` после собственного gain, поэтому панорама не вмешивается в pitch, depth, атаку, фактуру или общий компрессор;
- аккорд сохраняет прежний лимит шести голосов и добавляет максимум шесть дешёвых panner-узлов;
- эхо затухающего следа звучит у сохранённого окончания пути, а не у пересекающей его руки; клавиатурные стрелки используют то же отображение;
- при отсутствии `createStereoPanner()` voice graph подключается напрямую к mono master и сохраняет все три oscillator nodes, жесты и визуальную реакцию;
- offline shell обновлён до v9, доступное описание объясняет пространственную ось без зависимости от слухового эффекта.

**Проверка:** синтаксис всех JS, music/score unit-тесты, JSON и `git diff --check` прошли. Chromium 390×844 @2x подтвердил pointer-glide от −0,54 до +0,54, эхо сохранённого правого окончания +0,47 при пересечении рукой в центре, keyboard-pan −0,24 и ровно шесть panner-узлов для семи одновременных контактов; все значения остались в ±0,68. Shell v9 сохранил 11 ресурсов и полностью загрузился offline без runtime/console errors. Отдельный запуск с отключённым `createStereoPanner` создал полный трёхосцилляторный голос без ошибок. Прямая инспекция `output/pond-piano/spatial-water-mobile-12.png` не выявила обрезки или поломанной композиции; автоматическая image-инспекция осталась недоступна из-за ошибки извлечения account ID.

**Следующий шаг:** дать глубине Y один общий очень короткий водный reflection-bus: глубокие голоса должны получать чуть больше пространства, не теряя атаку, mono-совместимость и текущий шестиголосный мобильный бюджет.
